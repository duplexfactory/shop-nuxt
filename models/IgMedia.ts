export default interface IgMedia {
    code: string;
    pageId: string;
    caption?: string;

    takenAt: number;

    price?: number;
    patchPrice?: number;

    mediaUrl?: string; // Only from official.
}













