{
  "private": true,
  "scripts": {
    "deploy": "yarn build && cp package.deploy.json .output/ && wrangler publish",
    "dev": "nuxi dev",
    "build": "nuxi build",
    "start": "node .output/server/index.mjs"
  },
  "devDependencies": {
    "@vueuse/core": "^6.8.0",
    "nuxt-windicss": "^2.0.0",
    "nuxt3": "latest"
  },
  "dependencies": {}
}
